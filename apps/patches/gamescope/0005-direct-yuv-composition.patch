diff --git a/src/Backends/DRMBackend.cpp b/src/Backends/DRMBackend.cpp
index 887796f..5fbb58d 100644
--- a/src/Backends/DRMBackend.cpp
+++ b/src/Backends/DRMBackend.cpp
@@ -2452,7 +2452,14 @@ LiftoffStateCacheEntry FrameInfoToLiftoffStateCacheEntry( struct drm_t *drm, con
 		entry.layerState[i].crtcH = crtcH;
 		entry.layerState[i].opacity = frameInfo->layers.get( i ).opacity * 0xffff;
 		entry.layerState[i].ycbcr = frameInfo->layers.get( i ).isYcbcr();
-		if ( entry.layerState[i].ycbcr )
+		if (frameInfo->layers.get(i).tex && frameInfo->layers.get(i).tex->isPackedYuv444())
+		{
+			entry.layerState[i].ycbcr = true;
+			entry.layerState[i].colorEncoding = DRM_COLOR_YCBCR_BT2020;
+			entry.layerState[i].colorRange = DRM_COLOR_YCBCR_LIMITED_RANGE;
+			entry.layerState[i].colorspace = frameInfo->layers.get(i).colorspace;
+		}
+		else if ( entry.layerState[i].ycbcr )
 		{
 			entry.layerState[i].colorEncoding = drm_get_color_encoding( g_ForcedNV12ColorSpace );
 			entry.layerState[i].colorRange    = drm_get_color_range( g_ForcedNV12ColorSpace );
@@ -3256,7 +3263,7 @@ drm_prepare_liftoff( struct drm_t *drm, const struct FrameInfo_t *frameInfo, boo
 					amdgpu_transfer_function degamma_tf = colorspace_to_plane_degamma_tf( entry.layerState[i].colorspace );
 					amdgpu_transfer_function shaper_tf = colorspace_to_plane_shaper_tf( entry.layerState[i].colorspace );
 
-					if ( bYCbCr )
+					if ( bYCbCr && !frameInfo->layers.get(i).tex->isPackedYuv444() )
 					{
 						// JoshA: Based on the Steam In-Home Streaming Shader,
 						// it looks like Y is actually sRGB, not HDTV G2.4
diff --git a/src/rendervulkan.cpp b/src/rendervulkan.cpp
index 54e4efc..d482753 100644
--- a/src/rendervulkan.cpp
+++ b/src/rendervulkan.cpp
@@ -231,6 +231,8 @@ struct {
 	{ DRM_FORMAT_XBGR2101010, VK_FORMAT_A2B10G10R10_UNORM_PACK32, VK_FORMAT_A2B10G10R10_UNORM_PACK32, 4, false, false },
 	{ DRM_FORMAT_ARGB2101010, VK_FORMAT_A2R10G10B10_UNORM_PACK32, VK_FORMAT_A2R10G10B10_UNORM_PACK32, 4, true, false },
 	{ DRM_FORMAT_XRGB2101010, VK_FORMAT_A2R10G10B10_UNORM_PACK32, VK_FORMAT_A2R10G10B10_UNORM_PACK32, 4, false, false },
+	// Experimental packed BT.2020 limited-range YUV import. Components are U,Y,V,X.
+	{ DRM_FORMAT_XVYU2101010, VK_FORMAT_A2B10G10R10_UNORM_PACK32, VK_FORMAT_A2B10G10R10_UNORM_PACK32, 4, false, false },
 
 	{ DRM_FORMAT_R8, VK_FORMAT_R8_UNORM, VK_FORMAT_R8_UNORM, 1, false, true },
 	{ DRM_FORMAT_R16, VK_FORMAT_R16_UNORM, VK_FORMAT_R16_UNORM, 2, false, true },
@@ -1205,6 +1207,24 @@ void CVulkanDevice::compileAllPipelines(std::stop_token st)
 {
 	pthread_setname_np( pthread_self(), "gamescope-shdr" );
 
+	// Packed HDR YUV bypasses composition until an overlay or screenshot is
+	// requested. Compile these common variants off the presentation thread so
+	// that first use does not stop buffer releases for hundreds of milliseconds.
+	for (uint32_t layerCount = 1; layerCount <= 3; ++layerCount) {
+		for (uint32_t videoLayer = 0; videoLayer < layerCount; ++videoLayer) {
+			for (uint32_t outputEotf : { uint32_t(EOTF_Gamma22), uint32_t(EOTF_PQ) }) {
+				if (st.stop_requested()) return;
+				PipelineInfo_t key = { SHADER_TYPE_BLIT, layerCount, 1u << (16 + videoLayer), 0, 0,
+					GAMESCOPE_APP_TEXTURE_COLORSPACE_HDR10_PQ << (videoLayer * GamescopeAppTextureColorspace_Bits), outputEotf, false };
+				VkPipeline compiled = compilePipeline(layerCount, key.ycbcrMask, key.shaderType,
+					key.blurLayerCount, key.compositeDebug, key.colorspaceMask, key.outputEOTF, key.itmEnable);
+				std::lock_guard<std::mutex> lock(m_pipelineMutex);
+				if (!m_pipelineMap.emplace(key, compiled).second)
+					vk.DestroyPipeline(device(), compiled, nullptr);
+			}
+		}
+	}
+
 	std::array<PipelineInfo_t, SHADER_TYPE_COUNT> pipelineInfos;
 #define SHADER(type, layer_count, max_ycbcr, blur_layers) pipelineInfos[SHADER_TYPE_##type] = {SHADER_TYPE_##type, layer_count, max_ycbcr, blur_layers}
 	SHADER(BLIT, k_nMaxLayers, k_nMaxYcbcrMask_ToPreCompile, 1);
@@ -1255,7 +1275,12 @@ VkPipeline CVulkanDevice::pipeline(ShaderType type, uint32_t layerCount, uint32_
 	auto search = m_pipelineMap.find(key);
 	if (search == m_pipelineMap.end())
 	{
+		const uint64_t compileStart = get_time_in_nanos();
 		VkPipeline result = compilePipeline(layerCount, ycbcrMask, type, blur_layers, effective_debug, colorspace_mask, output_eotf, itm_enable);
+		const uint64_t compileNs = get_time_in_nanos() - compileStart;
+		if (compileNs > 10000000)
+			vk_log.infof("Pipeline compile: %.3f ms type=%u layers=%u ycbcr=%x colorspace=%x output=%u itm=%u",
+				double(compileNs) / 1000000.0, unsigned(type), layerCount, ycbcrMask, colorspace_mask, output_eotf, unsigned(itm_enable));
 		m_pipelineMap[key] = result;
 		return result;
 	}
@@ -4459,7 +4484,7 @@ std::optional<uint64_t> vulkan_composite( struct FrameInfo_t *frameInfo, gamesco
 		if (frameInfo->layers.count() >= 2 && frameInfo->layers.get( 1 ).zpos == g_zposOverride)
 			blur_layer_count++;
 
-		cmdBuffer->bindPipeline(g_device.pipeline(type, blur_layer_count, frameInfo->ycbcrMask() & 0x3u, 0, frameInfo->colorspaceMask(), outputTF ));
+		cmdBuffer->bindPipeline(g_device.pipeline(type, blur_layer_count, frameInfo->ycbcrMask() & 0x00030003u, 0, frameInfo->colorspaceMask(), outputTF ));
 		cmdBuffer->bindTarget(g_output.tmpOutput);
 		for (uint32_t i = 0; i < blur_layer_count; i++)
 		{
diff --git a/src/rendervulkan.hpp b/src/rendervulkan.hpp
index fd355d0..dc9a861 100644
--- a/src/rendervulkan.hpp
+++ b/src/rendervulkan.hpp
@@ -199,6 +199,11 @@ public:
 	inline EStreamColorspace streamColorspace() const { return m_streamColorspace; }
 	inline void setStreamColorspace(EStreamColorspace colorspace) { m_streamColorspace = colorspace; }
 
+	inline bool isPackedYuv444() const
+	{
+		return drmFormat() == DRM_FORMAT_XVYU2101010;
+	}
+
 	inline bool isYcbcr() const
 	{
 		return format() == VK_FORMAT_G8_B8R8_2PLANE_420_UNORM;
@@ -429,6 +434,9 @@ struct FrameInfo_t
 		{
 			if (layers.get( i ).isYcbcr())
 				result |= 1 << i;
+			// Upper bits distinguish packed YUV sampled through ordinary RGB views.
+			if (layers.get(i).tex && layers.get(i).tex->isPackedYuv444())
+				result |= 1u << (16 + i);
 		}
 		return result;
 	}
diff --git a/src/shaders/composite.h b/src/shaders/composite.h
index a62324e..e8ca201 100644
--- a/src/shaders/composite.h
+++ b/src/shaders/composite.h
@@ -182,7 +182,18 @@ vec4 sampleLayerEx(sampler2D layerSampler, uint offsetLayerIdx, uint colorspaceL
 
     uint colorspace = get_layer_colorspace(colorspaceLayerIdx);
     vec4 color;
-    if (get_layer_shaderfilter(offsetLayerIdx) == filter_pixel) {
+    if ((c_ycbcrMask & (1u << (16u + colorspaceLayerIdx))) != 0u) {
+        // The diagnostic XV30 client supplies BT.2020 NCL, limited-range PQ.
+        vec3 uyv = textureLod(layerSampler, coord, 0.0).rgb * 1023.0;
+        float y = (uyv.g - 64.0) / 876.0;
+        float cb = (uyv.r - 512.0) / 896.0;
+        float cr = (uyv.b - 512.0) / 896.0;
+        vec3 rgb = vec3(y + 1.4746 * cr,
+                       y - 0.164553 * cb - 0.571353 * cr,
+                       y + 1.8814 * cb);
+        color = vec4(colorspace_plane_degamma_tf(max(rgb, vec3(0.0)), colorspace), 1.0);
+    }
+    else if (get_layer_shaderfilter(offsetLayerIdx) == filter_pixel) {
         vec2 output_res = texSize / u_scale[offsetLayerIdx];
         vec2 extent = max((texSize / output_res), vec2(1.0 / 256.0));
         color = sampleBandLimited(layerSampler, coord, unnormalized ? vec2(1.0f) : texSize, unnormalized ? vec2(1.0f) : vec2(1.0f) / texSize, extent, colorspace, unnormalized);
@@ -193,6 +204,16 @@ vec4 sampleLayerEx(sampler2D layerSampler, uint offsetLayerIdx, uint colorspaceL
     else {
         color = sampleRegular(layerSampler, coord, colorspace);
     }
+    // Transparent overlay pixels contribute nothing to the blend. Avoid the
+    // matrix and colour-management LUT lookups for the empty overlay canvas.
+    // Opaque layers must still ignore alpha, and premultiplied emissive pixels
+    // with nonzero RGB must retain their contribution.
+    uint alphaMode = get_layer_alphamode(colorspaceLayerIdx);
+    if (color.a == 0.0 &&
+        (alphaMode == alpha_mode_coverage ||
+         (alphaMode == alpha_mode_premult && all(equal(color.rgb, vec3(0.0))))))
+        return vec4(0.0);
+
     // JoshA: AMDGPU applies 3x4 CTM like this, where A is 1.0, but it only affects .rgb.
     color.rgb = vec4(color.rgb, 1.0f) * u_ctm[colorspaceLayerIdx];
     color.rgb = apply_layer_color_mgmt(color.rgb, offsetLayerIdx, colorspace);
diff --git a/src/steamcompmgr.cpp b/src/steamcompmgr.cpp
index 1bf66e5..1802f8e 100644
--- a/src/steamcompmgr.cpp
+++ b/src/steamcompmgr.cpp
@@ -2738,6 +2738,7 @@ paint_window_commit( const gamescope::Rc<commit_t> &lastCommit, steamcompmgr_win
 
 	layer->eAlphaBlendingMode = ( flags & PaintWindowFlag::CoverageMode ) ? ALPHA_BLENDING_MODE_COVERAGE : ALPHA_BLENDING_MODE_PREMULTIPLIED;
 
+
 	return layer;
 }
 
@@ -5010,6 +5011,8 @@ static std::vector< steamcompmgr_win_t* > GetGlobalPossibleFocusWindows()
 static void
 steamcompmgr_xdg_determine_and_apply_focus( const std::vector< steamcompmgr_win_t* > &vecPossibleFocusWindows )
 {
+	g_steamcompmgr_xdg_focus.overlayWindow = nullptr;
+	g_steamcompmgr_xdg_focus.externalOverlayWindow = nullptr;
 	for ( auto &window : g_steamcompmgr_xdg_wins )
 	{
 		if (window->isOverlay)
@@ -9273,6 +9276,8 @@ void steamcompmgr_check_xdg(bool vblank, uint64_t vblank_idx)
 				pFocus->inputFocusWindow = nullptr;
 			if (pFocus->overlayWindow && pFocus->overlayWindow->type == steamcompmgr_win_type_t::XDG)
 				pFocus->overlayWindow = nullptr;
+			if (pFocus->externalOverlayWindow && pFocus->externalOverlayWindow->type == steamcompmgr_win_type_t::XDG)
+				pFocus->externalOverlayWindow = nullptr;
 			if (pFocus->notificationWindow && pFocus->notificationWindow->type == steamcompmgr_win_type_t::XDG)
 				pFocus->notificationWindow = nullptr;
 			if (pFocus->overrideWindow && pFocus->overrideWindow->type == steamcompmgr_win_type_t::XDG)
@@ -9287,6 +9292,8 @@ void steamcompmgr_check_xdg(bool vblank, uint64_t vblank_idx)
 		if (s_PipewireFocus.focusWindow && s_PipewireFocus.focusWindow->type == steamcompmgr_win_type_t::XDG)
 			s_PipewireFocus.focusWindow = nullptr;
 #endif
+		g_steamcompmgr_xdg_focus.overlayWindow = nullptr;
+		g_steamcompmgr_xdg_focus.externalOverlayWindow = nullptr;
 		g_steamcompmgr_xdg_wins = wlserver_get_xdg_shell_windows();
 		MakeFocusDirty();
 	}
