diff --git a/transport/dongle.c b/transport/dongle.c
index fb5494b..b5b5e55 100644
--- a/transport/dongle.c
+++ b/transport/dongle.c
@@ -1429,8 +1429,7 @@ static void xone_dongle_shutdown(struct usb_interface *intf)
 	if (dongle->fw_state != XONE_DONGLE_FW_STATE_READY)
 		dongle->fw_state = XONE_DONGLE_FW_STATE_STOP_LOADING;
 
-	if (system_state == SYSTEM_RESTART)
-		return;
+	/* Diagnostic: quiesce controllers for warm restart as for shutdown. */
 
 	err = xone_dongle_power_off_clients(dongle);
 	if (err)
